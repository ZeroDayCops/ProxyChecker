services:
  - type: web
    name: zdc-proxy-checker-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend_api.main:app --host 0.0.0.0 --port $PORT
    plan: free
